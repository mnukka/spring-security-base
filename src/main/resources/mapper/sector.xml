<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ee.company.crm.domain.persistence.sector.SectorDao">
    <select id="findAll" resultType="ee.company.crm.domain.persistence.sector.SectorEntity">
        SELECT *
        FROM sector
                 LEFT JOIN connectby('sector', 'id', 'parent_id', '1', 0, '->')
            AS t(h_id int, h_parent_id int, level int, ord text)
                           on h_id = sector.id
        WHERE enabled = 'true';
    </select>
</mapper>
